import Labels from '../Labels';

const ErrorCodes = {
  INVALID_ITEMS: {
    message: Labels.Game.StepOne.invalidInputErrorMessage,
  },
  MISSING_NUMBER_INVALID: {
    message: Labels.Game.StepTwo.invalidInputErrorMessage,
  },
};

export default ErrorCodes;
